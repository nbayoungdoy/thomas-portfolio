#include <stdio.h>

int main() {
	int length, width, area;	
	printf("emter length");
	scanf("%i", &length);
	printf("enter width");
	scanf("%i", &width);
	area = width * length;
	printf("%i", area);
	return 0;
}
